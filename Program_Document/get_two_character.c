
/* this program is written to accept and store two characters in different memory locations, and to display them one after the other using the functions
getchar () and putchar () */
/* writer: Chhatra Chhorm */
#include <stdio.h>
main()
{
	char c, d;
	c = getchar ;
	d = getchar ;
	fflush (stdin);
	putchar (c);
	putchar (d);
}

#include <stdio.h>
/* this program is written to find the ASCII value of character */
main ()
{
	int a, b, d;
	printf ("Enter the value of a, b");
	scanf ("%d", &a);
	scanf ("%d", &b);
	d = a + b;
	printf ("the sum of %d and %d is %d", a,b,d);
}

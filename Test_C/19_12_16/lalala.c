#include <stdio.h>
/* this program is written to get the characters from user until he or she gives ! as the input, and then compare the numbers of
non-vowel and vowel that have been given */
main ()
{
	int v, nv;
	char inp;
	v = nv = 0;
	inp = ' ';
	while (inp != '!')
	{
		puts ("Enter the character");
		inp = getchar ();
		fflush (stdin);
		switch (inp)
		case 'A':
			v++;
			break;
		case 'a':
			v++;
			break;
		case 'E':
		
		
	}
 } 


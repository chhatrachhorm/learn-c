#include<stdio.h>
//Binary Search
int main()
{
	int a[100], num
	printf("Enter number of elemet: "); scanf("%d", &num);
	for(i=0; i<num; i++)
	scanf("%d", a+i);
	puts("Do you want to search anything?(Y/N)");
	fflush(stdin); c = getchar();
	if(a=='Y'||a=='y')
	{
		puts("Enter what you want to search for: ");
		scanf("%d", &search);
		test = num / 2;
		while()
		{
			if(a[test]>)
		}
		
	}
	return 0;
}
